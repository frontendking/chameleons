import { Stack } from '../src/Stack'

describe('Stack', () => {
  let stack
  beforeEach(() => {
    stack = new Stack()
  })
  describe('crud', ()=>{
    it('push', ()=>{

    })
    it('pop')
    it('peek')
  })
})
